model:
  adapter:
    projector_dim: 256
    select: DeepModel # 可选: UtteranceLevel 和 DeepModel

    UtteranceLevel:
      pooling: MeanPooling

    DeepModel:
      model_type: CNNSelfAttention
      hidden_dim: 80
      kernel_size: 5
      padding: 2
      pooling: 5
      dropout: 0.4
      

